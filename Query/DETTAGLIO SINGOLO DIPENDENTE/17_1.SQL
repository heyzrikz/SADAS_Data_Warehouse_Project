--Stipendio annuo, calcolato su ultimo anno solare 

SELECT CODICE_FISCALE,ANNO,SUM(STIPENDIO)AS STIPENDIO_ANNUO,SUM(STIPENDIO_NETTO)AS STIPENDIO_NETTO_ANNUO
FROM CEDOLINI_SADAS_2023
GROUP BY CODICE_FISCALE,ANNO