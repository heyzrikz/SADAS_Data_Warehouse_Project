--Età media dei dipendenti raggruppati per filiale

SELECT ROUND(AVG(DATEDIFF('YY', data_nascita,TODAY)), 0) AS eta_media,FILIALE
FROM (SELECT DISTINCT CODICE_FISCALE, DATA_NASCITA,FILIALE FROM ANAGRAFICA
GROUP BY CODICE_FISCALE,DATA_NASCITA,FILIALE
)
GROUP BY FILIALE
