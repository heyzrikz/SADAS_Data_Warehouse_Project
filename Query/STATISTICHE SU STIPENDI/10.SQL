-- Stipendio medio mensile raggruppato per dipartimento

SELECT DISTINCT  DIPARTIMENTO, ROUND(AVG(STIPENDIO),2) AS STIPENDIO_MEDIO
FROM ( SELECT * FROM CEDOLINI_FILIALE_DIPARTIMENTO
        )
GROUP BY DIPARTIMENTO
